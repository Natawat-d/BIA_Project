# Copy to .env and adjust if needed.

# Path to the raw dataset (defaults to ../netflix_customer_churn.csv if unset)
NETFLIX_CSV=/Users/natawatd/Desktop/AIT/BIA_Project/netflix_customer_churn.csv

# Optional PostgreSQL BI layer (matches docker-compose.yml defaults)
PGHOST=localhost
PGPORT=5432
PGDATABASE=netflix_dss
PGUSER=netflix
PGPASSWORD=netflix
